---
description: End-to-end CREATE workflow with gated phases (clarify → explore → design → implement → verify → review → tests).
---

Use the Skill tool to invoke the `add-feature` skill. $ARGUMENTS
